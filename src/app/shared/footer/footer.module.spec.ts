import { FooterModule } from './footer.module';

describe('FooterModule', () => {
  const module: FooterModule = new FooterModule();

  it('should create', () => {
    expect(module).toBeTruthy();
  });
});
